[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[phases.build]
cmds = ["cd backend && python scripts/generate_grid.py && python ml/train.py"]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
